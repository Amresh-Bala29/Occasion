# NemoClaw

Runtime configuration for the sandboxed browser the agents use for computer-use
automation.

- `config.yaml` — runtime behaviour (headless, timeouts, viewport, screenshots).
- `permissions.yaml` — allow-listed domains and blocked actions.
- `network-policy.yaml` — egress rules and rate limits.
